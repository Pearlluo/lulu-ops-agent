{
  "_comment": "LuluAgent Admin panel state. Overrides model_registry.yaml roles at runtime. Format: 'provider/model' or null to use the registry default. two_stage: 'auto' = polish with the answer model only when it differs from the planner and is available; 'on' = always; 'off' = planner answers directly.",
  "planner_model": "openai/gpt-5-mini",
  "answer_model": "deepseek/deepseek-chat",
  "fallback_model": "openai/gpt-5-mini",
  "temperature": 0.1,
  "two_stage": "auto"
}
